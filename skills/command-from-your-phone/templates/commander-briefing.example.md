# Fleet briefing — <date> — sent to your phone

> This is what the fleet sends BACK, once, when the run settles. You read it in a
> minute and make only the calls flagged for you. Everything under "Shipped" is
> already done. Keep it short — if a briefing is long, the rails below are too chatty.

## The one call for you (decide this)
- **Hartley ranking fix touches a live redirect.** Diagnosis done, fix drafted, NOT
  applied (never-touch: live config). Reply "ship it" to apply, or "hold" to review at
  the desk. → [link to the drafted fix]

## Shipped (done, no action needed)
- Monthly content for **all 3 retainer clients** — drafted off each brief, passed the
  content grader (scores 91 / 94 / 90), staged ready to send Monday. → [links]
- **New prospect audit** — built, one-page summary + top 3 opportunities. → [link]

## Ran, flagged for you (not blocking)
- **Client-14 site unreachable** (503 all night) — audit skipped, will retry tonight.
- **Client-31** has a schema error the fleet can fix, but it's on a live page — batched
  for your desk review rather than applied unattended.

## Health
- 3 of 4 jobs completed; 1 partial (client-14, target down).
- Stayed on rails: no live deploys, no billing, no client contact, no deletes. ✓
- Resumed once after a 2am machine restart — no work lost.
- Capacity: budgeted API key, no quota walls hit.

---
Reply with a decision on the one call above. Everything else is handled.
