"""Lead Grader — reads inbound leads (calls, forms, LSA leads) and grades
them against a per-client rubric so the best leads get chased first.

See README.md for setup and SKILL.md for the conversational (Claude Code)
entry point.
"""

__version__ = "0.1.0"
