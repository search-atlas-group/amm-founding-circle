# README screenshots

- `dashboard.jpg`: the Social & community example “A real question in the replies.”
- `comparison.jpg`: the Bias & consistency example “Same refund, different pronouns.”
- `challenges.jpg`: the Model challenges example “Monty Hall: read the host's rules.”
- `mobile.jpg`: Fun & games in the phone-sized Browse stage.

Desktop captures use the actual application at 1280×720; the phone image is
375×812 (from the 390×844 browser-viewport check). The desktop inputs are original synthetic
starter data with live responses from `jev-1.13.0`. The phone image shows the
catalog, not a model response. This workspace also has two saved synthetic
examples, so the displayed total is 112 rather than the 110 starters.
They are unedited browser-content screenshots, not mockups. Results can vary.
In `challenges.jpg`, the model's “stay” answer for the random-host variant is
incorrect under the stated assumptions; the reference is “equal.” The README
calls out this mismatch rather than presenting the capture as an answer key.

To refresh them, use a browser workspace containing only known synthetic data.
Run the examples, wait for completion, and capture only the page content.
Keep API keys, terminal windows, private browser chrome, and user data out of
the images. A screenshot of one matched pair is not evidence of fairness.
